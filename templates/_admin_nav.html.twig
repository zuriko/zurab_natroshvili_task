<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm mb-4">
	<div class="container-fluid">
		<a class="navbar-brand fw-bold" href="{{ path('admin_dashboard') }}">Admin Panel</a>
		<div class="collapse navbar-collapse">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link{% if app.request.attributes.get('_route') starts with 'admin_news' %} active{% endif %}" href="{{ path('admin_news_index') }}">
						News
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link{% if app.request.attributes.get('_route') starts with 'admin_category' %} active{% endif %}" href="{{ path('admin_category_index') }}">
						Categories
					</a>
				</li>
			</ul>
			<form action="{{ path('app_logout') }}" method="post" class="ms-auto mb-0">
				<button type="submit" class="btn btn-outline-light btn-sm">
					<i class="bi bi-box-arrow-right"></i>
					Logout
				</button>
			</form>
		</div>
	</div>
</nav>
